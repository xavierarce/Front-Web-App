function IconCarList({ className, divClassName }) {
  return (
    <div className={`icon-car-list ${className}`}>
      <div className={`text-wrapper ${divClassName}`}>I</div>
    </div>
  );
}
export default IconCarList;
